local API = require (game. ServerScriptService:WaitforChild("IrelloAPI*))
local BanBoardID - API:GetBoardID ("Tutorial Bans")
local BaniistID - API:GetlistID ("Bans", BanBoardID)

function checkurrentPlayersForBan()
while wait (10) do
local BanCards - API:GetCardsInlist (BanlistID)
for
Player in pairs (game. Players: GetChildren())
do
For
Card in pairs (BanCards) do
Tr string. find (Card.name, Player.UgerId)
then
Plaver:Kick ("You have been banned.")
end
end
end
end
end
spawn (check
ForBan)
game. Players. PlayerAdded: Connect (function (plr)
local BanCards = API:GetCardsInLigt (BanListID)
for
Card in pairs (BanCards) do
if string. find (Card.name, pir.UserId) then
pir:Kick ("You have been Banned.")
end
end
end)
